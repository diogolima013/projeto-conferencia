# Copie este arquivo para ".env" APENAS para testar localmente com `netlify dev`.
# As chaves de verdade NUNCA devem ir para o Git — configure-as direto no
# painel do Netlify em: Site settings > Environment variables.

OMIE_APP_KEY=sua_app_key_aqui
OMIE_APP_SECRET=sua_app_secret_aqui
